// Who is allowed into the admin panel.
// Add/remove emails here — lowercase, one per line.
// IMPORTANT: also update the matching list in your Firestore rules
// (see README.md "Security rules") so uploads are enforced server-side too,
// not just hidden in the UI.

const ADMIN_EMAILS = [
  "mdriyadboss1234@gmail.com"
];